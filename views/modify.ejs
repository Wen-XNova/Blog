<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">

    <title>Bloggie Post</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>

  <div class="editContainer">
    <% if (locals.post) { %>
        <form id="editForm" method="post" action="/api/posts/<%=post.id%>">
          <input type="text" name="title" value="<%=post.title %>">
          <textarea name="content" rows="12"><%=post.content %></textarea>
          <input type="text" name="author" value="<%=post.author %>">
          <button class="full-width" type="submit">
            <%= submit %>
          </button>
        </form>
        <% } else {%>
          <form id="newPostForm" method="post" action="/api/posts">
            <input type="text" name="title" placeholder="Title" required>
            <textarea name="content" placeholder="Content" required rows="12"></textarea>
            <input type="text" name="author" placeholder="Author" required>
            <button class="width" type="submit">
              <%= submit %>
            </button>
          </form>
          <% } %>
  </div>

          <script src="/js/background.js"></script> 
</body>
</html>